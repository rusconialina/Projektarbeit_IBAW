export class BookRequest {
    constructor(titel, band, autors, date, rate) {
        this.titel = titel;
        this.band = band;
        this.authors = autors;
        this.date = date;
        this.rate = rate;
    }
}